let checkName = (firstName, lastName) => {
        if(firstName === 'majom' && lastName === 'csati') console.log('you are csati');
        else console.log('you are NOT csati');
    };

checkName('majom', 'csati');